# FastAPI and ASGI server
fastapi==0.128.8
uvicorn[standard]>=0.20.0

# HTTP client for NetSuite API calls
httpx>=0.24.0
# requests/urllib3 security fix (GHSA-gc5v-m9x4-r6x2, PYSEC-2026-141/142) needs Python>=3.10.
# Markers keep installs working on 3.9 and auto-patch once Python is upgraded.
requests>=2.33.0; python_version >= "3.10"
requests>=2.32.5; python_version < "3.10"
urllib3>=2.7.0; python_version >= "3.10"
requests-oauthlib>=1.3.0

# Data validation and serialization
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Environment variables management
# dotenv fix (GHSA-mf9w-mj56-hr94) needs Python>=3.10.
python-dotenv>=1.2.2; python_version >= "3.10"
python-dotenv>=1.0.0; python_version < "3.10"

# Date and time handling
python-dateutil>=2.8.0

# JSON handling and data processing
# orjson fix (GHSA-hx9q-6w63-j58v) needs Python>=3.10.
orjson==3.11.6; python_version >= "3.10"
orjson==3.11.5; python_version < "3.10"

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# CORS support for frontend integration
# python-multipart fixes (GHSA-wp53/.. up to GHSA-6jv3) need Python>=3.10; 0.0.20 is the
# last 3.9-compatible build, so on 3.9 these remain unpatched until Python is upgraded.
python-multipart==0.0.31; python_version >= "3.10"
python-multipart==0.0.20; python_version < "3.10"

# Auth
PyJWT==2.13.0
argon2-cffi==25.1.0
pydantic[email]

# Security floors for transitive deps (patched releases that DO support Python 3.9)
starlette>=0.49.1,<1.0.0
cryptography>=46.0.7
idna>=3.15

# Logging
structlog>=23.0.0

uvloop>=0.16.0
httptools>=0.5.0

# For background CSV writer (asyncio queue)
aiofiles>=23.0.0
